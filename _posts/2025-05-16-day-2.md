layout: post
title: "Day 2 - Getting Started"
---

### What I Set Out to Do
I wanted to complete the code for the functions that I will use in the word game. This does not include the code for the word_game() function itself.

---

### What I Actually Did

I completeted the code for the functions. I wrote two functions called check_letter() and check_word(). I also revised my code for the functions generate_letter() and generate_word() that I created the day before. check_letter() checks if the word provided by the user has the right letter parameters requested by generate_letter(). check_word() just checks if the word exists by seeing if the word is in the list of scrabble words. I chose the scrabble word list because it has obscure words.

---

### What I Learned or Noticed

I learned that planning the arguments and returns of each function is really helpful to see how the functions will interact with each other.

---

### What's Next

I plan on coding the main word_game() function next that will utilize all the functions I have already written.

